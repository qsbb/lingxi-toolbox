<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
      iOS26 风格拨动开关（圆形版）：
      28×28 正圆轨道 + 20×20 正圆滑块（滑块左右移动表达开/关）。
      铁律：Setter 直切状态（无 Storyboard/EnterActions）；尺寸固定正圆。
    -->
    <Style x:Key="LxToggleSwitch" TargetType="CheckBox">
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <Grid Width="28" Height="28">
                        <Ellipse x:Name="Track"
                                 Fill="{DynamicResource LxGlassFillBrush}"
                                 Stroke="{DynamicResource LxGlassBorderBrush}"
                                 StrokeThickness="1" />
                        <Ellipse x:Name="Thumb"
                                 Width="20" Height="20"
                                 Fill="{DynamicResource LxSwitchThumbBrush}"
                                 HorizontalAlignment="Left" Margin="2,0,0,0" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Track" Property="Fill" Value="{DynamicResource LxTintBrush}" />
                            <Setter TargetName="Track" Property="Stroke" Value="Transparent" />
                            <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right" />
                            <Setter TargetName="Thumb" Property="Margin" Value="0,0,2,0" />
                            <Setter TargetName="Thumb" Property="Width" Value="14" />
                            <Setter TargetName="Thumb" Property="Height" Value="14" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Track" Property="Opacity" Value="0.9" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
